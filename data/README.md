# Data Assets

This directory stores structured project data that is not narrative documentation and does not belong in app assets.

- `locations/wilayas-with-municipalities.json` - canonical Algeria location dataset used to generate Supabase seed SQL

Keep generated outputs elsewhere:

- Supabase seed SQL belongs in `backend/supabase/seeds/`
- helper scripts for regenerating datasets belong in `tool/`
